# Credentials — never commit. The example file is the exception.
.env
.env.*
*.env
.devin.env
!.env.example

# Local state
data/
work/
*.db
*.db-wal
*.db-shm

# ...except the committed evidence snapshot, which is the whole point of
# docs/evidence: a reviewer with no account can replay a real run.
!docs/evidence/run.db

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
