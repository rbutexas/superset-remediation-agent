──────────────────────────────────────────────────────────────────────────────
 REMEDIATION PIPELINE
 2026-09-20 15:32
──────────────────────────────────────────────────────────────────────────────

  3 finding(s) detected · 3 resolved · 0 outstanding

  OUTCOME MIX

    fixed                        2  ███████████████████·········
    mitigated                    1  █████████···················

  COST

    not yet reported by the API — Devin aggregates consumption
    on a delay, so 0.00 here means 'unknown', not 'free'.

  FINDINGS

    unawaited-user-event
      test(frontend): 205 un-awaited userEvent calls across 52 files, and th
      issue #4         status fixed                  time-to-verdict 30m
      PR  https://github.com/rbutexas/superset/pull/5
      ADMITTED GAP: The flakes described in the issue were not reproduced locally (all 15 suites passed on the base branch), so the fix is d

    unfinished-migration:simple-zstd
      deps(frontend): simple-zstd is pinned pending a code change that was n
      issue #2         status fixed                  time-to-verdict 5m
      PR  https://github.com/rbutexas/superset/pull/3
      ADMITTED GAP: Did not run the real `npm run dev-server` against a Flask backend serving content-encoding: zstd HTML (no backend in thi

    unresolvable-advisory:xlsx
      security-triage: xlsx advisories cannot be resolved by upgrade and wil
      issue #1         status mitigated              time-to-verdict 11m
      PR  https://github.com/rbutexas/superset/pull/7
      ADMITTED GAP: The suppression's effect on the scanner (remediation-agent / OSV-based) was not observed; that scanner does not consume 

  HEURISTIC vs AGENT

    the cheap rule matched the agent on 2/3
    where they differ is where the judgement actually happened

  ADMITTED GAPS — work the agent declined to claim as verified

    unfinished-migration:simple-zstd: Did not run the real `npm run dev-server` against a Flask backend serving content-encoding: zstd HTM
    unawaited-user-event: The flakes described in the issue were not reproduced locally (all 15 suites passed on the base bran
    unresolvable-advisory:xlsx: The suppression's effect on the scanner (remediation-agent / OSV-based) was not observed; that scann

──────────────────────────────────────────────────────────────────────────────